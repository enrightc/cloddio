// game.js — Core game logic
// Stage 1: placeholder only, no logic implemented

// Entry point — called once the DOM is ready
document.addEventListener('DOMContentLoaded', () => {
  console.log('Cloddio — Stage 1 skeleton loaded');
});
